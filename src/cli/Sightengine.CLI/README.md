# Sightengine.CLI

Command-line interface for the Sightengine SDK generated with AutoSDK.

## Installation

```bash
dotnet tool install --global Sightengine.CLI --prerelease
```

## Usage

```bash
sightengine --help
sightengine api --help
```